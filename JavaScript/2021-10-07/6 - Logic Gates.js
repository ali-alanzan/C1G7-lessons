// Logic Gates
/**
 * always works with boolean
 * &&: when using && symbol must be all the conditions are true
 * ||: when using || symbol expect only one condition to be true
 */


var condition = true && true && true; // true
var condition = true && false && true; // false

console.log(condition);



var condition = false || false || true; // true

console.log(condition);